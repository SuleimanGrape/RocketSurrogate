"""Shared, dependency-light modules used by all three packages.

- schema:  single source of truth for input/output field names and encodings
- scalers: StandardScaler / MinMaxScaler (numpy-only)
- dataio:  JSONL loading helpers
"""
