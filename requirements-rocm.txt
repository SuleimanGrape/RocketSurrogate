# ── Core: ROCm-compatible PyTorch (install FIRST, on the AMD/ROCm machine) ────
# Latest ROCm build on the PyTorch index is torch 2.9.1 + rocm6.4 (verified
# 2026-06-18); wheels exist for CPython 3.10–3.14. Install with:
#
#   pip install torch==2.9.1 torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.4
#
# rocm6.3 carries the same 2.9.1 if your ROCm runtime is 6.3:
#   pip install torch==2.9.1 ... --index-url https://download.pytorch.org/whl/rocm6.3
#
# NOTE: ROCm wheels are Linux-only — there is no native-Windows ROCm PyTorch.
# Run training on the Linux/ROCm box (or WSL2 with a ROCm-supported AMD GPU).
#
# For NVIDIA instead: use a CUDA wheel. Blackwell cards (RTX 50xx, sm_120) need
# cu128 or newer — cu124 will NOT support them:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

# ── Data & ML ────────────────────────────────────────────────────────────────
numpy>=1.26
scipy>=1.14
scikit-learn>=1.5
joblib>=1.4
pandas>=2.2

# ── XGBoost (local CPU training) ─────────────────────────────────────────────
xgboost>=2.0

# ── LLM fine-tuning (cloud ROCm) ─────────────────────────────────────────────
transformers>=4.44
trl>=0.11
peft>=0.13
accelerate>=0.35
bitsandbytes>=0.44       # 0.43+ has native ROCm support
sentencepiece>=0.2       # tokenizers for many LLM architectures
protobuf>=5.28

# ── Optional but recommended ──────────────────────────────────────────────────
# flash-attn>=2.6        # faster attention; build from source for ROCm
# wandb>=0.18            # experiment tracking
# tensorboard>=2.17      # local metric visualization
